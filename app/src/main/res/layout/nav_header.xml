<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="@dimen/header_height"
                android:background="@mipmap/background"
                android:gravity="center"
                android:orientation="horizontal">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/weather_icon"
        android:layout_width="@dimen/zhihudetailsinfo_bottom"
        android:layout_height="@dimen/zhihudetailsinfo_bottom"
        android:layout_margin="@dimen/margin"/>

    <TextView
        android:id="@+id/weaher_area"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/weather_icon"
        android:layout_margin="@dimen/margin"
        android:layout_toRightOf="@id/weather_icon"
        android:drawablePadding="@dimen/margin"
        android:drawableRight="@mipmap/edit"
        android:textColor="@color/white"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/weaher_area"
        android:layout_margin="@dimen/margin"
        android:layout_toRightOf="@id/weather_icon"
        android:orientation="vertical">

        <TextView
            android:id="@+id/weather_type"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/white"/>

        <TextView
            android:id="@+id/weather_temperature"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin"
            android:textColor="@color/white"/>
    </LinearLayout>
</RelativeLayout>